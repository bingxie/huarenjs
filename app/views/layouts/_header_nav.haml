#header-menu-toggle-menu.toggle-menu.header-toggle-menu-menu.hidden

  = nav_v_link('www', t("menu.home"), 'home')

  = nav_v_link('auused', t("menu.used"), 'new_listing')

  = nav_v_link('aurent', t("menu.rent"), 'information')

  = nav_v_link('ausocial', t("menu.social"), 'feedback')

  = nav_v_link('aulife', t("menu.life"), 'feedback')

  = nav_v_link('aujob', t("menu.job"), 'invite')
